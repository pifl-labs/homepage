# Approved app icon synchronization — 2026-09-18

Scope: Focus, Words, DDay, Log, Draw, Word Voyage approved source icons; home catalog, fleet log, localized app landing, TikTok hub. Existing Hello/Bridge/other icons and all app copy/store facts remain unchanged.

Root cause: website-owned icon.webp assets were never synchronized with the approved app artwork. Assets use one-year immutable caching. New source-hash filenames avoid relying on visitors clearing their cache; historical assets remain available.

Provenance: src/data/app-icons.json records approved original PNG SHA256 and derived 256x256 WebP SHA256. Conversion uses cwebp quality92, no redesign/crop. All four consumers use one resolver. Tests check all six asset hashes, versioned paths, unchanged fallbacks and consumer coverage.

Verification:
- npm test: 120 passed.
- npm ci --ignore-scripts restored missing existing node_modules package; package-lock unchanged.
- npm run build: 133 static pages.
- npm run test:built: store links, Draw facts, Focus transition passed after correcting a test-script shebang placement; initial failures retained outside repository.
- Browser: 3 locales × 5 widths (320/375/768/1280/1920) × 2 themes: all six catalog icons loaded at expected paths; no horizontal overflow.
- 18 localized mobile app details: matching icon loaded. Hub images loaded. KO 375px 200% root font sizing: no horizontal overflow. Actual catalog→Focus navigation passed.
- Visual: approved source originals inspected; changed catalog icons fit existing square bounds without distortion. Before/after same viewport captures retained externally. Unchanged typography/layout is not a new whole-site quality certification.
- Local evidence: /Users/pirate/pifl-labs/docs/analysis/2026-09-18-homepage-icons/LOCAL-QA.json and PNGs.

Public deployment: pending at commit time; must verify remote rendered URLs and asset bytes after merge. Known separate issue: homepage names/version facts beyond Focus remain stale; this patch does not claim to update store status or publish app binaries.
